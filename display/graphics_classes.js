class Label {
    x, y, text;
    surface = CHARS,
        update = false,
        bound_left = 0,
        bound_right = CHARS_ACROSS,
        bound_top = 0,
        bound_bottom = CHARS_DOWN,
        reverse_x = false;

}
